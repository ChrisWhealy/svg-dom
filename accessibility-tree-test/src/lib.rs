//! Empty on purpose — this crate exists only to host the `accessibility_tree` integration test under `tests/`.
//! See that file's module doc comment for what it verifies and why it lives in its own on-demand workspace member
//! rather than in the main crate's `tests/` directory.
