//! Tests for `paint`'s own demos.
//! One file per demo here, mirroring `paint`'s own one-file-per-demo layout.

mod blend;
mod color_matrix;
mod component_transfer;
mod filter;
mod light_sources;
mod lighting;
mod linear_gradient;
mod morphology;
mod radial_gradient;
mod turbulence;
